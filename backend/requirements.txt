# Kept in sync with pyproject.toml by hand, for anyone using plain pip.
# uv.lock is the source of truth for exact, reproducible versions.

fastapi==0.115.0
uvicorn[standard]==0.30.6
sqlalchemy==2.0.35
pydantic==2.9.2
pydantic-settings==2.5.2
python-jose[cryptography]==3.3.0
passlib==1.7.4
# Pinned — passlib 1.7.4 (last release, unmaintained) breaks with bcrypt>=4.1.
# See: https://github.com/pyca/bcrypt/issues/684
bcrypt==4.0.1
python-multipart==0.0.9
python-dateutil>=2.9.0
# pandas 2.2.2 predates numpy 2.x and segfaults in to_datetime when paired
# with a current numpy, taking the whole process down. Keep these together.
pandas>=2.3.0
numpy>=2.1.0
pdfplumber==0.11.4
scikit-learn==1.5.2
python-dotenv==1.0.1
email-validator==2.2.0
